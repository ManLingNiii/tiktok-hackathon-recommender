# Track Template

Copy this directory to start a new track:

```bash
cp -r tracks/_template tracks/<your_track_name>
```

Do not modify `tracks/bpr/`. Follow the shared rules in `configs/common.yaml`, reuse `src/data.py` and `src/evaluate.py`, and put detailed outputs under `results_by_track/<your_track_name>/`.
